import BaseGrid from './base-grid'

/**
 * Component implementing the vertical grid feature. When this component is available for a widget, its API is exposed
 * via the {.xGrid} namespace. It inherits all methods from the [BaseGrid]{@link ../components/base-grid.html}.
 *
 * @function XGrid
 */
export default BaseGrid('x')
